# Tmux
alias ta='tmux attach -t'
alias ts='tmux new-session -s'
alias tl='tmux list-sessions'
alias tksv='tmux kill-server'
alias tkss='tmux kill-session -t'

# LS
alias lsor='ls -la | grep "^d" && ls -la | grep "^-" && ls -la | grep "^l"'

# Apt-cyg REPO
alias cyg='apt-cyg mirror http://mirrors.kernel.org/sources.redhat.com/cygwin/'
alias cyp='apt-cyg mirror http://mirrors.kernel.org/sources.redhat.com/cygwinports/'

# other
alias ca='clear && source ~/.zsh/art.zsh'

# folder
alias downloads='cd c:/users/inetpwnz/downloads'
alias sbot='cd c:/Program\ Files/SilkroadEurope/sbot'
alias dev-code='cd d:/developing/code'

# bot


# Programms
alias osu='cd c:/users/inetpwnz/appdata/local/osu! && run osu!'
